# 仓库开发规范

本仓库只维护独立的多相机/Piper 手眼标定工具。

- 新增说明、配置注释和 CLI 文案使用简体中文；代码标识符保持英文。
- 所有代码、文档、示例和测试只使用本工具自身的通用术语与占位值。
- Python API 保持调用方中立，不内置特定业务配置 schema、路径或写回逻辑。
- README 只描述本工具的 API 合同，不提供跨 schema 字段投影或适配调用编排示例。
- 运行时模块只依赖标准库、已声明依赖和受支持的硬件 SDK。
- 不提交真实相机序列号、frame、CAN 映射、机器人网络信息、凭据、会话图像或标定结果。
- Piper 代码只能读取状态和法兰反馈；禁止加入运动、使能、失能、复位、急停或夹爪控制。
- 制品导出、解析和测试必须共同维护同一份固定合同。
- 修改后至少运行 `python -m pytest`、`python -m build`、`git diff --check` 和敏感信息扫描。
