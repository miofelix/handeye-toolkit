# 第三方依赖声明

本仓库不复制或打包以下硬件 SDK：

- `orbbec/pyorbbecsdk`：Apache License 2.0；DaBai DC1 采集的可选运行依赖。
- `IntelRealSense/librealsense`（`pyrealsense2`）：Apache License 2.0；D435 彩色流采集的可选运行依赖。
- `agilexrobotics/pyAgxArm`：GNU LGPL v3；Piper 只读反馈的可选运行依赖。

NumPy、SciPy、OpenCV、Pillow、PyYAML 和其他 Python 依赖仍分别受其上游许可证约束。
安装或再分发依赖时，使用者应遵守对应上游许可证。
